# Point unlock and snap categories — September8,2026

Revisione82b9e4. Same native Front1293×743 / clone Top landscape887×736 and
mouse Peekaboo input as constrained-drag receipt. Evidence root workspace
`reports/openshape3d-core-sketch-milestone-2026-09-08/selection/`.

## Point lock removal: paired sampled pass

Native selected lockedleft401,455, used right-rail Unlock, dragged vertically:
line401–661 moves toy412, retains horizontal and length; other rectangles stable.
Clone selected lock glyph with edge click173,512 (first center click selected
point instead); scrolled palette to expose Delete and removed only lock. Drag
left171,515 upward yields line171–383,y485,3.5mm retained. Native direct Unlock
versus clone glyph/Delete is an interaction difference, not identical UI.
`native-unlocked-drag.png`, `clone-unlocked-drag.png`.

## Snap versus Auto-Constrain: paired sampled pass

Before native: Grid+Snapping Hints on, sketch guidelines/guidepoints/3D/distant
edges off, Auto-constraining on. Clone initially Grid/SketchGuidepoints/FaceGuidepoints/
Hints/Auto-Constrain on. Native separate snap and constraint popovers; clone rail
settings opens a sheet. Captured before states. Existing settings not assumed equal.

Turned all acquisition categories and Auto-Constrain off, left Hints on. Native
new line400,560→564,551,570.3217mm follows slight raw slope, no H. Clone valid line
181,573→345,564,2.7mm follows raw slope, no H. No source geometry moved.
`native-grid-auto-off-line.png`, `clone-grid-auto-off-line-valid.png`.
Invalid clone attempt armed Rectangle because palette remained scrolled; retained
`clone-grid-auto-off-line.png` is explicitly NOT line evidence. It leaves a small
2.698×0.147 rectangle in the disposable design. Scroll CLI failed foreground
validation; GUI palette drag restored the top. No service/permission changes.

Enabled only Grid, leaving Auto-Constrain and other acquisition off. Native new
line401–560,y657,550mm snaps to grid without H. Clone verified expanded settings
`clone-grid-on-auto-off-settings.png`, then new line474,545→655,515,3.041mm snaps
to grid increments without H. Different grid scales/origins; semantic comparison,
not identical coordinates or slope. `native-grid-only-line.png`,
`clone-grid-only-line-valid.png`. Initial partially-expanded switch click did not
change Grid; original/repeat `clone-grid-only-line*.png` without `valid` are NOT
Grid-on evidence. Their states are retained rather than promoted to passes.

No new product change or test run for these manual acceptance samples. Broader
snap category independence, zoom/3D and live history still open. Current settings:
both Grid+Hints on, Auto-Constrain off; other native snap categories off; clone
SketchGuidepoints/FaceGuidepoints off. Reopen preference check/publication pending.

Clone gallery reopen retained Grid/Hints on, guidepoints/Auto-Constrain off;
`clone-snap-reopen-settings.png`. Clone-only preference persistence result.
Restored original settings after sample: native Grid/Hints/Auto-Constrain on,
other acquisition off; clone all four toggles and Auto-Constrain on. Captures
/tmp/os3d-settings-restored.png and /tmp/os3d-native-settings-restored.png inspected.

Publication BLOCKED/PENDING09:31: siximages visible in existing Doc UI, but saving
taking longer than usual, editing temporarily disabled. Anonymous export remains
76images and lacks six new hashes; preserved tab without reload/duplicate inserts.
`published-snap-lock.docx`, `snap-lock-publication.json` records failed verification.
Local evidence complete; continue independent trim checks while sync resolves.
