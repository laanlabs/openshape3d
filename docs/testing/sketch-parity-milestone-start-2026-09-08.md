# Milestone start and history/input diagnosis — September 8, 2026

Baseline `cd0937d`, no app source modifications. Read the supplied six-step plan,
all project rules, implementation ledger and latest three-point receipt. Retained
all 42 audit records and mapped all 56 QA scenarios without promoting partial
historical coverage into a pass. Prior unrelated identity files left untouched.

## Reconciliation

No xcodebuild/xctest/Peekaboo worker active at 06:50 EDT. Historical checkpoint
had contradictory dirty/running claims; archived and replaced. Installed simulator
executable SHA256 `70781f75b6bd975f604b9a5fb847a8cef48f4a8a3ffe26f26c0062359c162810`
matching prior receipt. Dedicated simulator initially on Home. New disposable
clone project `Untitled 2` created through gallery plus, not store reset.

Peekaboo image capture failed with web-focus mutation-outcome error. Supported
`see --no-web-focus --path` saved usable images, each inspected. Full-screen see
returned failure despite saving an image: inspected artifact is evidence, not a
claim that the entire command passed. Local capture lacks Screen Recording;
no permissions/security/services changed.

## H-01: fresh clone rectangle history — unresolved

Portrait simulator, mouse through Peekaboo, ground plane; native Shapr3D macOS
reference (existing disposable audit project). Clone diagonal rectangle 3×2 mm
created at window corners approximately (221,336) and (462,496), both readouts.
Undo at window (253,118) leaves geometry unchanged. Full desktop confirms target
is unobstructed and Simulator frontmost. Cmd-Z and global synthesized click at
(528,162) also leave geometry unchanged. Other navigation/menu/canvas operations
work. No source-level history defect is established by these inputs.

Native Cmd-N presented trial offer; Skip dismissed it without trial/purchase.
Reference remains the previous disposable audit project, not a fresh project.
Menu changes to Diagonal work, but several drag/click attempts move only the
point indicator, not a rectangle. These are failed delivery/workflow attempts,
not reference rectangle semantics. Native Cmd-Z removed the existing upper-right
vertical segment; Cmd-Shift-Z restored it. Paired fresh creation/dimension/history
and after-reopen history remain blocked/incomplete, not signed off.

Evidence in workspace `reports/openshape3d-core-sketch-milestone-2026-09-08/history/`:
clone-created / toolbar-undo / keyboard-undo / global-undo / full-desktop;
native-trial-dismiss / click-rectangle / synth-result / keyboard-undo / keyboard-redo.
No repeat of identical clicks planned. Next independent work: regression, original
acceptance mapping and pending publication. Later paired input needs fresh known
working canvas-delivery route; no speculative Undo change.

## Regression result

Serial run `/tmp/os3d-milestone-history-20260908.xcresult`, log same prefix;
LineChainUITests plus gallery-reopened rectangle Undo/profile test. Simulator
exclusively reserved for this run; no Peekaboo interaction during tests. Completed
07:00 EDT: 2/2 passed in one clean run, zero failures. Gallery profile Undo/Redo
and four-step tap-chain history pass under XCTest. No test runner remains active. This is diagnostic coverage, not final all-suite candidate acceptance.

## Documentation

Detailed milestone plan and complete QA map local. Prior illustrated Google Doc
verified 33 images; final September 7 gallery reopen note/image publication being
completed. Current milestone publication pending verification. Physical Pencil,
installable device build and candidate readiness remain unverified.
