# Existing dimension through alternate palette entry — September 9, 2026

Baseline3df7dec pushed. Native final saved Ø1000 label opens a closed-lock editor
(previous lock-action receipt). Clone saved Ø0.992 label does likewise, but
Constrain → Dimension on that same selected circle leaves no keypad even after
settling and hides its radial control. This is an alternate clone entry path,
not a claim native has an identical Constrain palette. Native reference is the
existing-size edit workflow already captured in the paired lock receipt.

Diagnosis screenshots: /tmp/os3d-dimension-palette-menu.png and
/tmp/os3d-dimension-palette-existing-settled.png. Code targeted literal candidate
labelID, but existing dimensions suppress that temporary label. Correction
routes matching kind/reference sets to beginDimensionEdit(storedLabel), retaining
its identity and shared editor/lock behavior. New unit checks actual geometric
re-edit, same dimension ID/count, undo and removal. Circle UI strengthens the
existing numeric workflow with alternate palette edit8/Undo10.

Serial46352 running: DimensionKeypadCommitTests plus circle diameter and
immediate-lock UI, exclusive simulator. No live post-fix claim yet.

Serial46352 completed0, clean9/9 (7unit+2UI). No runner remains. Fresh live
Front circle430450: ordinary diameter commit1, Constrain→Dimension now opens
visible closed-lock editor1. Commit2 preserves center, Undo1/Redo2 inspected.
Gallery reopen/Sketch1, reselectleft350450 and same palette route displays2
with closed lock. Native existing1000→2000 comparison under capture.

## September 9 — alternate Dimension entry for stored sizes

Constrain → Dimension used a suppressed candidate label when a stored size
already existed, leaving no visible keypad. It now reuses the matching stored
label/ID. Clean9/9 (7unit+2UI). Live circleØ1→Ø2, unchanged center, Undo/Redo
and gallery reopen with palette entry verified; native existingØ1000→Ø2000
center/history and final reopened closed-lock editor paired. Native reference
is size editing, not an identical alternate palette. Illustrated135 images/all5
new hashes and master38 final reopen note export-verified.
[Receipt](testing/sketch-parity-dimension-palette-2026-09-09.md).
